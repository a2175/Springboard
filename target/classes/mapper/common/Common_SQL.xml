<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="common">

	<select id="selectFileInfo" parameterType="hashmap" resultType="hashmap">
    	<![CDATA[
        	SELECT
            	STORED_FILE_NAME,
            	ORIGINAL_FILE_NAME
        	FROM
            	tb_file
        	WHERE
            	IDX = #{IDX}
    	]]>
	</select>

</mapper>
<!--
SELECT
	AAA.*
FROM
	(
	SELECT
		COUNT(*) OVER() AS TOTAL_COUNT,
		AA.*
	FROM
		(
		SELECT
			ROW_NUMBER() OVER (ORDER BY IDX DESC) RNUM,
			IDX,
			TITLE,
			HIT_CNT,
			CREA_DTM
		FROM
			TB_BOARD
		WHERE
			DEL_GB = 'N'
		) AA
	) AAA
WHERE
	AAA.RNUM BETWEEN #{START} AND #{END};
	
SELECT
	AAA.*
FROM
	(
	SELECT
		COUNT(*) AS TOTAL_COUNT,
		AA.*
	FROM
		(
		SELECT
			IDX,
			TITLE,
			HIT_CNT,
			CREA_DTM
		FROM
			TB_BOARD
		WHERE
			DEL_GB = 'N'
		ORDER BY IDX DESC
		) AA
	) AAA
LIMIT 0, 20;

SELECT A.* FROM (
SELECT COUNT(*) AS TOTAL_COUNT, TITLE FROM TB_BOARD
GROUP BY IDX ORDER BY IDX
) A LIMIT 0, 20;

SELECT 
	COUNT(*) AS TOTAL_COUNT,
	A.* 
FROM
	(
	SELECT 
		IDX,
		HIT_CNT,
		CREA_DTM 
	FROM 
		TB_BOARD
	
	) A
	ORDER BY IDX DESC
	LIMIT 0, 20;

SELECT
	TITLE,
	IDX,
	HIT_CNT,
	CREA_DTM 
FROM 
	TB_BOARD
ORDER BY IDX DESC
LIMIT 535, 540;

SELECT * FROM (
SELECT COUNT(*) AS TOTAL_COUNT, 조회컬럼 FROM TABLE
ORDER BY 조건
) LIMIT START, 게시글 수 

-->
